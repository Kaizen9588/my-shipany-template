# 2026-08-30

- 完成首批生产安全整改：P0-3 默认管理员弱口令。迁移 0012 取消公开默认管理员；新增 0019 禁用历史固定 hash 账号；仅显式 `ADMIN_BOOTSTRAP_EMAIL` 才会创建一次性 `pending_activation` 管理员，首次强制改密后激活。
- 迁移执行增加 PostgreSQL 事务级 advisory lock，避免多实例并发执行 DDL；管理员引导、类型检查、针对性测试和相关 lint 均通过。
- 方案文档、README 与环境变量模板已同步初始管理员的新安全流程。
- 完成迁移发布安全第二批：唯一建库入口改为 `pnpm migrate`，运行时只做版本校验并在缺失迁移时 fail-fast；迁移器保留单事务 advisory lock，且对旧 `install.sql` 留下的无版本 `users` 基线做检测。新增迁移元数据单测，完整测试 45 文件 / 185 用例、类型检查与定向 lint 通过。
- 新增 `docs/IMPLEMENTATION-HANDOFF-2026-08-30.md` 作为 Agent 交接入口，列出已完成项、按优先级的 No-Go/P1/P2/P3 欠账、代码位置、验证结果和建议执行顺序。
- 第二批：P0-2 积分并发扣减（迁移 0020 用户级事务 advisory lock + `credit-concurrency.test.ts` 并发回归）、N-1 通知密钥脱敏（`toNotifyConfigView` 只回 set 标志+末四位掩码）、P0-4 匿名 demo 防绕过（输入 413 计次 / 退还仅限无上游费用错误 / 当日失败封顶 / `rateLimit()` fail-closed）。顺带修复 `pnpm migrate` 在 Node type-stripping 下因相对导入缺 `.ts` 后缀崩溃。验证：tsc / 48 文件 198 用例 / 定向 lint 全through。文档已同步 docs/03、05、14、boundary-spec 与交接文档。
- ⚠️ 遗留：目标 Supabase 开发库 pooler 报 `tenant not found`（疑似项目暂停），迁移 0020 未应用、真实并发用例未跑；环境恢复后先 `pnpm migrate` + `TEST_DATABASE_URL` 跑 `credit-concurrency.test.ts`，完成前 P0-2 视为未关闭、不得开放真实收费。
- 第三批（N-2 部分 / N-3）：`models/db.ts` 拆 `serverClient()`/`userClient()`，资金/支付/退款/后台统计路径切 `serverClient()`，兼容入口 `getSupabaseClient()` 保留；新增 `__tests__/db-rbac-static.test.ts` 静态断言兜住资金 RPC 调用点误走 anon。N-3 关闭，N-2 库级（private schema + REVOKE PUBLIC + service_role）待连库执行。验证：tsc / 48 文件 198 用例 / 静态断言 / lint 通过。文档已同步 boundary-spec 与交接文档。
- 第四批（P0-1 部分 / N-13 部分）：新增迁移 `0021_refund_debt_dispute.sql`（`credit_debts` 欠款账本 + `refunds` 退款单 + `orders.status` 扩展 `refund_requested/refund_blocked/disputed/charged_back` + 存储过程 `debt_regulate_order_refund` 超扣差额写欠款 + 订单 `refund_blocked` + 账号 `restricted`）。`services/refund.ts` 的 `processRefund` 在扣回量 < 订单发放积分时自动触发债务化并 trackCriticalEvent 记欠款额。N-13：`PaymentEventType` 新增 `dispute_opened/dispute_won/dispute_lost`，新增 `services/dispute.ts` 归一化争议状态机（disputed 冻结 / dispute_won 解冻 / charged_back+restricted），`lib/payment/index.ts` 转发。测试：`__tests__/refund.test.ts`（债务化分支）+ 新增 `__tests__/dispute.test.ts`。验证：tsc / **50 文件 210 用例**（3 个并发用例因未设 TEST_DATABASE_URL 跳过）/ 定向 lint 通过。
- ⚠️ 遗留：迁移 0020/0021 均未应用（待连库，当前 Supabase 开发库 `tenant not found`）。P0-1 剩余 webhook `refund_requested` 中间态 + credit_lots 精确批次准入校验；N-13 剩余 provider 解析器归一化 dispute（stripe/creem/waffo 当前只 emit `refund_succeeded`）+ 联盟奖励冻结 + 收入确认。完成前 P0-1/N-13 仍按部分关闭对待，不得开放真实收费。
- 第五批（纯代码，N-13 完成 + N-5 + 定价真相源）：①N-13 渠道解析器归一化 dispute——Stripe `charge.dispute.created/closed`(won/lost) 全链路，Creem `dispute.created`→opened，Waffo Pancake v0.19 无 dispute webhook 事件（仅 Dashboard `notifyChargeback`，已知边界）；②N-5 fail-closed——`rateLimitUser` 未配置/抖动时回落内存日窗口（不再 `{ok:true}`），checkout 加 per-IP/per-user 限流，webhook 三路由加 body 64KB 上限（`lib/webhook-guard.ts`，刻意不做 IP 限流：渠道来源 + TRUSTED_PROXY=none 同桶会误拒真实支付）；③P1-8 定价真相源钉死 = `payment_products` 表运行时权威，`data/pricing.ts` 仅种子/回退，admin payment-products 写入加不变量（金额/积分/有效期上限、币种仅 USD、积分≤金额），P1-定价-1 升 P0 后关闭。验证：tsc / **53 文件 226 用例**（3 个并发用例跳过）/ lint 通过。新增测试：`provider-dispute.test.ts`、`webhook-guard.test.ts`、`payment-products-guard.test.ts`。
- ⚠️ 遗留更新：N-5、N-13、P1-定价-1（P0-定价-1）已关闭。剩余连库项：应用 0020/0021、N-2 库级权限；剩余纯代码/运营项：N-4 outbox、N-6 高风险操作审批、P0-1 剩余、N-13 剩余（联盟奖励冻结/争议收入确认）、P0-对账/inbox、AI 请求状态机。
- 第六批（纯代码，N-6 高风险操作强制理由）：新增 `lib/admin-reason.ts`（`parseReason`：trim 后 5~200 字符）；6 个 admin 路由服务端强制 reason 并写入 `audit_logs.detail`——退款 `/api/admin/refund`（Stripe 成功+manual 双路径落审计）、积分调账 `/api/admin/user/credits`、角色/封禁 `/api/admin/user`（nickname 豁免）、定价 `/api/admin/payment-products`、支付渠道 `/api/admin/payment-settings`、告警密钥 `/api/admin/notify-settings`。后台 UI 全部同步：RefundButton 改 prompt 收集理由、credits adjust 页 remark 升级为必填 reason（Textarea）、users/[uuid] 三个 server action（角色/封禁/调账）parseReason 校验+必填输入框、pricing/payment/notify 三表单前端预校验+随 PUT 提交。users 页 server action 直调 service 的路径不经过 API 路由，也加了同一校验。测试：payment-products-guard 补 reason+新增缺 reason 拒绝用例、notify-settings-mask 三处 PUT 补 reason。验证：tsc / **53 文件 227 用例**（3 个并发用例跳过）/ lint 0 errors。
- ⚠️ N-6 剩余（归迁移批次）：审批队列/双人复核（需新表）+ 管理员操作 outbox 持久化（依赖 N-4）。boundary-spec N-6 行与 handoff §1.12/§3/§5 已同步。
- 第七批（纯代码，P0-1 webhook 中间态 + 副作用 after() 调度）：①迁移 `0022_webhook_refund_registration.sql`——新增 `register_order_refund_request` 存储过程（refunds 退款单 + 订单置 refund_requested 中间态 + 债务化准入，provider_refund_id/同订单 pending 幂等，已终态订单只登记不动状态），`process_order_refund` 扩展接受 refund_requested 并同步 pending 退款单；②`services/refund.ts` 新增 `registerRefundRequest`（webhook 专用，登记即 warn 告警），`lib/payment/index.ts` 的 `refund_succeeded` 改为只登记不扣积分/不终态化（缺 user_uuid 不登记，critical 告警人工核查），admin 退款路径保持 `processRefund` 直回收+终态；③`PaymentEvent` 新增 `provider`/`provider_ref_id`，stripe（charge.refunds.data[0].id）/creem（obj.id）/waffo（顶层 event.id，SDK 幂等 ID）三适配器填充；④新增 `lib/after-response.ts` `runAfterResponse()`（Next 16 `after()`，请求作用域外回退裸后台），audit/oplog（recordOpEvent 改 void+告警外呼）/email/payment 通知邮件全部接入——解决 P1-A「告警根本没机会跑」，outbox（N-4）持久化重试仍归迁移批次。测试：payment-event 改登记断言+缺 user_uuid 用例、refund 新增 registerRefundRequest 3 用例、db-rbac-static fundsRpcs 扩为 5 个资金 RPC（断言改逐 serverClient 分段）、oplog mock next/server after。验证：tsc / **53 文件 232 用例**（3 个并发用例跳过）/ lint 0 errors。文档同步 docs/05 §4.3+P0-退款-1、docs/16 P1-A、boundary-spec N-10、handoff §1.14/1.15/§3/§5。
- ⚠️ 遗留更新：迁移 0020/0021/0022 均未应用（待连库）。P0-1 剩余 credit_lots 精确批次准入 + 后台回收工作台；N-4 outbox、N-6 审批队列、webhook inbox/对账、AI 请求状态机均需新表。**纯代码能做的批次已基本见底，下一步是连库统一做迁移**（Supabase 库恢复后 `pnpm migrate` 应用 0020/0021/0022 → N-2 库级权限 → TEST_DATABASE_URL 跑并发用例）。
- 第八批（对抗式审查未提交改动 + 修复）：审查发现 P0×2/P1×5/P2×4 并全部修复。**P0-死锁**：0022 初版登记时调 `debt_regulate(p_refunded_credits=0)` 会把未回收的全额积分打成欠款+refund_blocked，而 `process_order_refund` 不接受 refund_blocked → 订单永久无法闭合；修复为登记不做债务化，债务化统一由闭合方 `processRefund` 按实际扣回量判定。**P0-双重退款**：admin 退款路由对 refund_requested 订单识别中间态只本地闭合（processRefund），绝不二次调渠道。P1：Waffo 退款反查订单补 user_uuid；`runAfterResponse` 按 E468/E91 区分静默回退 vs 显式告警；`fireAndForgetAudit` 的 IP 在注册时解析（回调内 headers() 抛错会丢整条审计）；parseReason 清洗零宽字符（U+200B-200D/FEFF/2060/00AD）防绕过；webhook-guard 流式实测 body（不信 content-length）+ `requestWithRawBody` 重建验签。P2：`lib/pricing-guard.ts` 定价校验抽共用（payment-settings JSON 旁路缺反套利）；processRefund 去重复审计+reason 透传（审计只在 admin 路由层落一条）；checkout per-IP 限流仅 TRUSTED_PROXY=cloudflare/vercel 时启用（none 时 IP 恒 127.0.0.1 全站单桶误拒买家）；trackCriticalEvent 动态 import 加 catch；内存限流 Map 惰性清理+1 万条上限（防随机 IP 撑爆）。验证：tsc / **54 文件 239 用例**（3 并发跳过）/ lint 0 errors（124 warnings 与 master 持平）。handoff §1.16 已同步。
